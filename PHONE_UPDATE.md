# Обновление телефона студии

**Актуальный номер:** +7 (992) 148-03-93

## Что исправлено

### Старый номер `+7 (999) 214-80-39` заменён везде на `+7 (992) 148-03-93`:

1. **templates/base.html** — футер на всех страницах
2. **templates/home.html** — добавлен блок с контактами внизу главной
3. **utils/notifications.py** — вынесена константа `STUDIO_PHONE`, используется в уведомлениях клиенту
4. **accounts/management/commands/send_reminders.py** — напоминания за день

## Бонусом

- Телефон во всех шаблонах теперь **кликабельный** (`<a href="tel:+79921480393">`) —
  на мобильных сразу предлагает позвонить
- В `utils/notifications.py` телефон вынесен в константу `STUDIO_PHONE` —
  меняется в одном месте
- В уведомлениях клиенту и админу добавлена пометка про снятие старой плёнки

## Если где-то ещё найдётся старый номер

Ищи так (в папке проекта):

```

findstr /s /i "9992148039" *.html *.py

```

Должно быть пусто.
