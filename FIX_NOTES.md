# Что исправлено (13.09.2026)

## 🎯 Главная ошибка
**TemplateSyntaxError: Unclosed tag on line 92: 'block'** — файл `templates/bookings/create.html`
был **оборван** посреди JS-кода, из-за чего `{% block extra_js %}` не имел закрывающего `{% endblock %}`.

Файл полностью переписан: JS-код завершён, все блоки закрыты.

## 🔧 Дополнительные исправления (рассинхрон БД ↔ интерфейс)

### 1. `bookings/models.py`
- Добавлено поле `tint_service` (FK на `services.TintService`)
- Обновлён `service_name`: теперь показывает конкретную услугу/пакет
- Добавлен `related_name='bookings'` для удобных обратных связей

### 2. `bookings/migrations/0002_booking_tint_service.py` (новый файл)
Миграция добавляет поле `tint_service` в таблицу Booking.

### 3. `bookings/forms.py`
- Добавлено поле `tint_service` (выбор услуги тонировки из новой таблицы)
- Написан `clean()` — проверка, что при тонировке выбраны услуга+плёнка,
  а при бронировании — пакет (и очистка «чужих» полей)

### 4. `bookings/views.py`
- **Главное:** `_calculate_price` теперь считает тонировку по формуле:
  `base_price(TintService) × коэф_модели × коэф_кузова × коэф_плёнки + снятие старой`
- `cars_data` теперь отдаёт: `tint_coefficient`, `armor_coefficient`, `body_coefficient`
- Вынесена константа `REMOVAL_TINT_PRICE = 500` (можно потом вынести в БД)
- Улучшены сообщения об ошибках формы
- После создания редирект на историю записей (раньше падало на `booking_detail`, шаблона нет)

### 5. `templates/bookings/create.html` (полностью переписан)
- JS завершён и работает
- Калькуляция **локальная** (без запросов к API — быстрее и надёжнее)
- Формат «×коэффициент» показывается рядом с плёнкой
- Кнопки времени блокируются, если слот занят
- Нельзя выбрать прошедшую дату

### 6. `accounts/models.py`
- Удалено битое свойство `car_class` (поля `car_class` больше нет в модели CarModel)
- Добавлены удобные свойства `tint_coefficient`, `armor_coefficient`, `body_type`

### 7. `templates/accounts/profile.html`
- Убрана ссылка на несуществующий `car_class.code`
- Вместо неё показываются коэффициенты модели (тонировка, бронь)

### 8. `templates/services/price_list.html`
- Убрана ссылка на несуществующий `pkg.coefficients.all`
- Добавлен блок «Услуги тонировки» (из новой таблицы TintService)
- Добавлен блок «Отдельные услуги бронирования» (ArmorElement)

### 9. `bookings/admin.py`
- В админке теперь видно `tint_service` и детали услуги
- В list_display добавлена колонка `service_detail` с названием услуги/плёнки

## ⚠️ Что нужно сделать после распаковки

```bash
# 1. Применить миграцию (добавит поле tint_service)
python manage.py migrate

# 2. Если нужно — перезалить начальные данные
python manage.py load_initial_data

# 3. Запустить
python manage.py runserver
```

## 📝 Что ещё предстоит сделать (не в этом фиксе)

1. **Пример расчёта коэффициентов плёнок** — нужно обсудить с пользователем
(какие коэффициенты у Spectroll / SunControl / атермальной)
2. **Отдельная форма бронирования элементов** (бампер, капор и т.п.) — пока только пакеты
3. **Тесты** калькулятора цен
4. **Вынести `REMOVAL_TINT_PRICE` в БД** (модель `AdditionalService` или подобное)

